#include <iostream>
#include <cmath>

using namespace std;

bool isEqual(float num1, float num2)
{
    return abs(num1) == abs(num2);
}

/* The function return 1 if the numbers are equal and 0 if they'r not */

/* int main()
{
    float a = 0.0f;
    float b = 0.0f;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    cout << isEqual(a, b);

    return 0;
} */